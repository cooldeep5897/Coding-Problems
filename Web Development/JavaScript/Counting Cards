var count = 0;

function cc(card) {
  // Only change code below this line
if(card>1 && card<7)count=count+1;
else if(card==10 ||card=='J'  ||card=='Q' || card=='K' ||card=='A')count=count-1;
  else{

  }
    let a="Hold";
    if(count>0)a="Bet"
    return count+" "+a;
  
}
